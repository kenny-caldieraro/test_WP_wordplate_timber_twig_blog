<!DOCTYPE html>
<html {{site.language_attributes}}>
	<head charset="{{site.charset}}">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		{{function('wp_head')}}
	</head>
	<body class="{{body_class}}">
		{% include "./partials/_menu.twig" %}
		<div class="container ">
			{% block body '' %}
		</div>
	</body>
	{{function('wp_footer')}}
</html>
